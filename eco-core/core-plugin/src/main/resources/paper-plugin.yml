name: ${pluginName}
version: ${version}
main: com.willfp.ecomenus.EcoMenusPlugin
api-version: 1.19

dependencies:
  - name: eco
    required: true
    bootstrap: false

load-after:
  - name: eco
    bootstrap: false
